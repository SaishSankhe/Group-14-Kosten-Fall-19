<h1 class="display-2 text-white font-weight-bold">CS-546-B Final Project</h1>

<ul class="list-unstyled">
    <div class="card card-stats col-md-6 mb-4 mb-lg-0 mt-3 mb-3">
        <div class="card-body">
            <li class="text-muted heading-small text-sm">
                <span class="font-weight-bold text-primary">{{groupMembers.groupNo}}</span>
                <br/>
                {{#each groupMembers.members}}
                    <span class="font-weight-bold text-primary">{{details}}</span> <br/>
                {{/each}}
            </li>
        </div>
    </div>
</ul>